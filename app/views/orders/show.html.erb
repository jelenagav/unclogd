<h1>Order</h1>
<ul>
  <li>Status: <%= @order.status %></li>
  <li>Amount: <%= humanized_money_with_symbol(@order.amount) %></li>
  <% @order.package.items.each do |item| %>
  <div class= "row">
  <%= render 'items/item_card', {item: item} %>
  </div>
  <% end %>

 <p> <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-name="My Productssssds"
    data-email="<%= current_user.email %>"
    data-description="HI"
    data-amount="<%= @order.amount_cents %>"
    data-currency="<%= @order.amount.currency %>"></script></p>
<ul>


